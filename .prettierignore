.idea/
.vscode/
api/
esm2022/
fesm2022/
node_modules
npm
RELEASE.md
src/

ondewo-proto-compiler

ondewo-vtsi-api
ondewo-vtsi-client-angular.d.ts
ondewo-vtsi-client-angular.d.ts.map
ondewo-vtsi-client-angular.metadata.json
package-lock.json
package.json
npm/
dist/
.test-build/
.test-build-examples/

# Codegen output, like api/ and fesm2022/ above: index.d.ts is ng-packagr's 3.8 MB rollup of the
# typings and public-api.ts is the generated barrel. `.husky/pre-commit` runs prettier in WRITE
# mode, so anything missing here is reformatted on every commit and no longer matches what the
# codegen emits. coverage/ is jest's report — untracked, and enough to fail `make prettier`.
index.d.ts
public-api.ts
coverage/
# README.md is prettier-ignored: .prettierrc sets useTabs, so prettier re-tabs the fenced
# code blocks that markdownlint's MD010 then de-tabs, and the husky hook runs prettier -w
# before pre-commit -- the two fight on every commit that touches the file. Prettier also
# rewrites the `[comment]: <> (START/END OF GITHUB README)` slice markers the release cuts
# the published README with.
README.md
